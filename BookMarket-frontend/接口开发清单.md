# Java后端接口开发清单

## 📋 按优先级排序的接口列表

---

## �🔥 第一阶段：首页核心接口（优先级：最高）

### 1. 获取热门分类
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/categories`
- 认证: 不需要

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "literature",
      "name": "文学小说",
      "icon": "📖",
      "count": 2345
    }
  ]
}
```

**实现步骤**:
1. 创建 `CategoryVO` 类
2. 在 `BookService` 中实现 `getCategories()` 方法
3. 可以返回静态数据，也可以从数据库统计
4. 在 `BookController` 中添加接口

**代码示例**:
```java
@GetMapping("/categories")
@Operation(summary = "获取热门分类")
public ApiResponse<List<CategoryVO>> getCategories() {
    return bookService.getCategories();
}
```

---

### 2. 获取热门书籍
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/hot`
- 认证: 不需要

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| limit | Integer | 否 | 返回数量，默认4 |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "JavaScript高级程序设计",
      "author": "Nicholas C. Zakas",
      "cover": "https://example.com/book1.jpg",
      "condition": "90",
      "price": 45.00,
      "originalPrice": 99.00,
    }
  ]
}
```

**实现步骤**:
1. 在 `BookRepository` 中添加查询方法
2. 在 `BookService` 中实现 `getHotBooks(limit)` 方法
3. 按销量排序，返回前N本书
4. 在 `BookController` 中添加接口

---

### 3. 获取书籍列表
**接口信息**:
- 方法: `GET`
- 路径: `/api/books`
- 认证: 不需要

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认12 |
| category_id | String | 否 | 分类筛选 |
| priceMin | Double | 否 | 最低价格 |
| priceMax | Double | 否 | 最高价格 |
| condition | String | 否 | 新旧程度 |
| keyword | String | 否 | 搜索关键词 |
| sortBy | String | 否 | 排序方式 |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "JavaScript高级程序设计",
        "author": "Nicholas C. Zakas",
        "categoryId": "computer",
        "condition": "九成新",
        "price": 45.00,
        "originalPrice": 99.00,
        "stock": 1,
        "cover": "https://example.com/book1.jpg",
      }
    ],
    "total": 2456,
    "page": 1,
    "pageSize": 12
  }
}
```

**实现步骤**:
1. 创建 `BookQueryRequest` 类接收参数
2. 在 `BookRepository` 中实现动态查询
3. 在 `BookService` 中实现 `getBooks(request)` 方法
4. 处理排序逻辑
5. 在 `BookController` 中添加接口


---

###  4.1 获取书籍基础信息
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/{id}`
- 认证: 不需要

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "JavaScript高级程序设计",
    "author": "Nicholas C. Zakas",
    "isbn": "9787115545381",
    "publisher": "人民邮电出版社",
    "publishDate": "2020-09",
    "pages": 896,
    "categoryId": "computer",
    "condition": "九成新",
    "price": 45.00,
    "originalPrice": 99.00,
    "stock": 1,
    "cover": "https://example.com/book1.jpg",
    "images": ["https://example.com/book1.jpg"],
    "description": "本书是JavaScript开发者必备的经典著作",
    "sellerId": 1,
    "sellerName": "书香阁",
    "sellerLevel": "金牌卖家",
    "sellerRating": 4.9,
    "isVerified": true,
    "salesCount": 2341,
  }
}
```

### 4.2获取卖家信息
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/{id}/seller`  <!-- 这里的 id 指sellerId,从后端返回的书籍基础信息中sellerId获取 -->
- 认证: 不需要

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "书香阁",
    "avatar": "https://example.com/avatar.jpg",
    "level": "金牌卖家",
    "isVerified": true,
    "rating": 4.9,
    "salesCount": 2341,
    "positiveRate": 99.2,
    "description": "专注计算机技术类书籍"
  }
}
```

**实现步骤**:
1. 在 `BookRepository` 中添加查询卖家信息方法
2. 在 `BookService` 中实现 `getBookSeller(id)` 方法
3. 在 `BookController` 中添加接口


---

### 4.3 获取评价信息
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/{id}/reviews` <!-- 这里的 id 指的是book的id -->
- 认证: 不需要

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认10 |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "userId": 2,
        "userName": "张三",
        "userAvatar": "https://example.com/avatar2.jpg",
        "rating": 5,
        "content": "书籍成色很好，和描述一致，推荐购买！",
        "createdAt": "2024-01-10T15:30:00Z"
      }
    ],
    "total": 128,
    "page": 1,
    "pageSize": 10
  }
}
```

---

### 5. 搜索书籍
**接口信息**:
- 方法: `GET`
- 路径: `/api/books/search`
- 认证: 不需要

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| keyword | String | 是 | 搜索关键词 |
| page | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认12 |

**实现步骤**:
1. 使用JPA的 `@Query` 注解实现全文搜索
2. 搜索字段：title、author、isbn
3. 支持分页

---

## 🔐 第二阶段：认证接口（优先级：高）

### 6. 用户注册
**接口信息**:
- 方法: `POST`
- 路径: `/api/auth/register`
- 认证: 不需要

**请求参数**:
```json
{
  "phone": "13800138000",
  "code": "123456",
  "username": "张三",
  "password": "123456",
  "email": "zhangsan@example.com"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "张三",
      "phone": "13800138000",
      "email": "zhangsan@example.com",
      "avatar": "https://example.com/avatar.jpg",
      "level": "普通会员",
      "points": 0
    }
  }
}
```

**实现步骤**:
1. 创建 `RegisterRequest` 类
2. 验证验证码（从Redis获取）
3. 检查手机号是否已注册
4. 检查用户名是否已存在
5. 密码加密（BCrypt）
6. 保存用户信息
7. 生成JWT Token
8. 返回Token和用户信息

---

### 7. 用户登录
**接口信息**:
- 方法: `POST`
- 路径: `/api/auth/login`
- 认证: 不需要

**请求参数**:
```json
{
  "account": "13800138000",
  "password": "123456",
  "type": "password"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "张三",
      "phone": "13800138000",
      "avatar": "https://example.com/avatar.jpg",
      "level": "普通会员",
      "points": 2580,
      "isSeller": false
    }
  }
}
```

**实现步骤**:
1. 创建 `LoginRequest` 类
2. 根据account类型（手机号/邮箱/用户名）查询用户
3. 验证密码（BCrypt）
4. 生成JWT Token
5. 返回Token和用户信息

---

### 8. 发送验证码
**接口信息**:
- 方法: `POST`
- 路径: `/api/auth/send-code`
- 认证: 不需要

**请求参数**:
```json
{
  "phone": "13800138000",
  "type": "register"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "验证码已发送",
  "data": null
}
```

**实现步骤**:
1. 创建 `SendCodeRequest` 类
2. 生成6位随机验证码
3. 将验证码存入Redis（5分钟过期）
4. 调用短信服务发送验证码
5. 返回成功

**Redis存储**:
```java
String key = "sms:code:" + phone + ":" + type;
redisTemplate.opsForValue().set(key, code, 5, TimeUnit.MINUTES);
```

---

### 9. 用户登出
**接口信息**:
- 方法: `POST`
- 路径: `/api/auth/logout`
- 认证: 需要

**响应示例**:
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

**实现步骤**:
1. 将Token加入黑名单（Redis）
2. 返回成功

---

## 👤 第三阶段：用户接口（优先级：中）

### 10. 获取用户信息
**接口信息**:
- 方法: `GET`
- 路径: `/api/user/profile`
- 认证: 需要

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "张三",
    "phone": "13800138000",
    "email": "zhangsan@example.com",
    "avatar": "https://example.com/avatar.jpg",
    "level": "普通会员",
    "points": 2580,
    "isSeller": false,
    "createdAt": "2024-01-15T10:00:00Z"
  }
}
```

**实现步骤**:
1. 从JWT Token中获取用户ID
2. 查询用户信息
3. 返回用户数据

---

### 11. 更新用户信息
**接口信息**:
- 方法: `PUT`
- 路径: `/api/user/profile`
- 认证: 需要

**请求参数**:
```json
{
  "username": "李四",
  "email": "lisi@example.com"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "username": "李四",
    "email": "lisi@example.com"
  }
}
```

---

## 🛒 第四阶段：购物车接口（优先级：中）

### 12. 获取购物车
**接口信息**:
- 方法: `GET`
- 路径: `/api/cart`
- 认证: 需要

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "bookId": 1,
      "book": {
        "id": 1,
        "title": "JavaScript高级程序设计",
        "author": "Nicholas C. Zakas",
        "cover": "https://example.com/book1.jpg",
        "condition": "90",
        "price": 45.00,
        "originalPrice": 99.00,
        "stock": 1,
        "sellerId": 1,
        "sellerName": "书香阁"
      },
      "quantity": 1,
      "price": 45.00,
      "createdAt": "2024-01-15T10:00:00Z"
    }
  ]
}
```

**实现步骤**:
1. 从JWT Token中获取用户ID
2. 查询购物车列表
3. 关联查询书籍信息
4. 返回购物车数据

---

### 13. 添加到购物车
**接口信息**:
- 方法: `POST`
- 路径: `/api/cart`
- 认证: 需要

**请求参数**:
```json
{
  "bookId": 1,
  "quantity": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "id": 1,
    "bookId": 1,
    "quantity": 1
  }
}
```

**实现步骤**:
1. 验证书籍是否存在
2. 验证库存是否充足
3. 检查购物车是否已有该书
4. 有则更新数量，无则新增
5. 返回购物车项

---

### 14. 更新购物车
**接口信息**:
- 方法: `PUT`
- 路径: `/api/cart/:id`
- 认证: 需要

**请求参数**:
```json
{
  "quantity": 2
}
```

---

### 15. 删除购物车项
**接口信息**:
- 方法: `DELETE`
- 路径: `/api/cart/:id`
- 认证: 需要

---

## ⭐ 第五阶段：收藏接口（优先级：低）

### 16. 获取收藏列表
**接口信息**:
- 方法: `GET`
- 路径: `/api/favorites`
- 认证: 需要

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认10 |

---

### 17. 添加收藏
**接口信息**:
- 方法: `POST`
- 路径: `/api/favorites`
- 认证: 需要

**请求参数**:
```json
{
  "bookId": 1
}
```

---

### 18. 取消收藏
**接口信息**:
- 方法: `DELETE`
- 路径: `/api/favorites/:id`
- 认证: 需要

---

## 📊 开发进度跟踪

### 第一周：基础框架 + 核心接口
- [ ] 创建Spring Boot项目
- [ ] 配置数据库和Redis
- [ ] 创建数据库表结构
- [ ] 实现统一响应类
- [ ] 实现JWT工具类
- [ ] 实现认证拦截器
- [ ] 实现获取热门分类接口
- [ ] 实现获取热门书籍接口

### 第二周：书籍接口 + 认证接口
- [ ] 实现获取书籍列表接口
- [ ] 实现获取书籍详情接口
- [ ] 实现搜索书籍接口
- [ ] 实现用户注册接口
- [ ] 实现用户登录接口
- [ ] 实现发送验证码接口
- [ ] 实现用户登出接口

### 第三周：用户接口 + 购物车接口
- [ ] 实现获取用户信息接口
- [ ] 实现更新用户信息接口
- [ ] 实现获取购物车接口
- [ ] 实现添加到购物车接口
- [ ] 实现更新购物车接口
- [ ] 实现删除购物车项接口

### 第四周：收藏接口 + 测试优化
- [ ] 实现获取收藏列表接口
- [ ] 实现添加收藏接口
- [ ] 实现取消收藏接口
- [ ] 编写单元测试
- [ ] 接口联调测试
- [ ] 性能优化
- [ ] 部署上线

---

## 🎯 开发建议

### 1. 优先实现首页接口
首页是用户最先看到的页面，建议优先实现：
- 获取热门分类
- 获取热门书籍
- 获取书籍列表

### 2. 使用Mock数据快速开发
在数据库数据不足时，可以先使用Mock数据：
```java
public ApiResponse<List<CategoryVO>> getCategories() {
    List<CategoryVO> categories = new ArrayList<>();
    categories.add(new CategoryVO("literature", "文学小说", "📖", 2345));
    // ...
    return ApiResponse.success(categories);
}
```

### 3. 使用Swagger文档
配置Knife4j后，访问 `http://localhost:8080/api/doc.html` 进行接口测试。

### 4. 异常处理
使用全局异常处理器统一处理异常：
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)
    public ApiResponse<?> handleBusinessException(BusinessException e) {
        return ApiResponse.error(e.getCode(), e.getMessage());
    }
}
```

### 5. 日志记录
使用Slf4j记录关键操作日志：
```java
@Slf4j
@Service
public class BookServiceImpl implements BookService {
    public ApiResponse<List<Book>> getHotBooks(Integer limit) {
        log.info("获取热门书籍，limit={}", limit);
        // ...
    }
}
```

---

## 📞 技术支持

如有问题，请参考：
- [Java后端开发指南.md](./Java后端开发指南.md) - 完整的开发指南
- [API接口文档.md](./项目指导文件/06-API接口文档.md) - 完整的API文档
- Spring Boot官方文档
- Spring Data JPA文档
